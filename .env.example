# Pool Cue v2 — Environment Variables
# Copy to .env and fill in values

# PostgreSQL connection string (Render provides this automatically)
# Leave blank for local dev — falls back to in-memory storage
DATABASE_URL=

# Public URL for QR codes (set to your Render URL in production)
BASE_URL=http://localhost:3000

# PIN for bartender/admin actions (session start/close, player removal)
SESSION_PIN=0000

# Password for the admin panel (/admin) — manage bars, ads, reports
ADMIN_PASSWORD=poolcue2026

# Environment flag (Render sets this automatically)
# Controls SSL mode for PostgreSQL connections
NODE_ENV=development

# Port (Render assigns automatically, defaults to 3000 locally)
PORT=3000
