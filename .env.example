# Pool Cue v2 — Environment Variables
# Copy to .env and fill in values

# PostgreSQL connection (Render provides this)
# Leave blank for local dev (falls back to in-memory storage)
DATABASE_URL=

# Public URL (used for QR codes)
BASE_URL=http://localhost:3000

# PIN for bartender actions (session start/close, remove player)
SESSION_PIN=0000

# Set to 'production' on Render
NODE_ENV=development
